export interface ThemeColorsProps {
  id: string;
  siteContentId: string;
  primaryColor: string;
  secondaryColor: string;
  hover: string;
  star: string;
  danger: string;
  price: string;
  title: string;
  textColor: string;
  oldPrice: string;
  borderColor: string;
  textButton: string;
  bgCard: string;
  themeColor: string;
  themeSelected: boolean;
  textHover: string;
}